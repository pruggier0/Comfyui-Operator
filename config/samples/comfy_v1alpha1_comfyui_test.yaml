apiVersion: comfy.redhat.com/v1alpha1
kind: ComfyUI
metadata:
  labels:
    app.kubernetes.io/name: comfyui-operator
    app.kubernetes.io/managed-by: kustomize
  name: comfyui-test
spec:
  # Use a real working image
  image: yanwk/comfyui-boot:cu121

  # Single replica for testing
  replicas: 1

  # No resource limits (so it can schedule on minikube)
  # resources:
  #   requests:
  #     cpu: "500m"
  #     memory: "1Gi"
